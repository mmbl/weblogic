user_home_dir: /home/coder
repo: "{{ user_home_dir }}/repo"
_user: coder
_group: coder
# связанные параметры, для удобства восприятия
# выделен параметр для явного указания JAVA_HOME
_files_jdk: "jdk-8u212-linux-x64.tar.gz"
_java_version: "1.8.0_212"
# =============== #
_files_fmw: "fmw_12.1.3.0.0_wls.jar"

# Для реализации WLS
oracle_base: "{{ user_home_dir }}/oracle"
JAVA_HOME: "{{ user_home_dir }}/jdk{{ _java_version }}"
MW_HOME: "{{ oracle_base }}/middleware1213"
weblogic_home: '{{ MW_HOME }}/wlserver'
mw_installer_folder: '{{ MW_HOME }}/install'
domains_home: "{{ MW_HOME }}/user_projects/domains"
# ====================== #
# параметры домена
DOMAIN_NAME: 'fpl_domain'
DOMAIN_USER: 'weblogic'
DOMAIN_USER_PSWD: 'wela@138'
NODE_MAN_USER: 'nmadmin'
NODE_MAN_USER_PSWD: 'nmadmin12345'
NODE_NAN_LISTEN: 5513
SERVER_NAME: 'AdminServer'
LISTEN_PORT: 7013
LISTEN_IP: '10.28.80.138'
SLAVE_LISTEN_IP: '10.28.80.139'